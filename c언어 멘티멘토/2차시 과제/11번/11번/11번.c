#include <stdio.h>
int main()
{
	int a[100] = { 0 };
	int x, ten = 0, one = 0;

	for (int i = 0; i < 100; i++)
	{

		printf("ют╥б");
		scanf_s("%d", &x);
		if (x = 0)
		{

		}
		else if(x>10)
		{
			ten++;
		}
		else if(0<x<)
	}
}